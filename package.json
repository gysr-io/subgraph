{
  "name": "gysr-subgraph",
  "license": "MIT",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ gysr-io/gysr",
    "create-local": "graph create --node http://localhost:8020/ gysr-io/gysr",
    "remove-local": "graph remove --node http://localhost:8020/ gysr-io/gysr",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 gysr-io/gysr"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.19.0",
    "@graphprotocol/graph-ts": "0.19.0",
    "ganache-cli": "^6.12.1",
    "truffle": "^5.1.55"
  }
}
